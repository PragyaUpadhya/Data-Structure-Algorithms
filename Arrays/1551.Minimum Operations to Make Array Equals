class Solution {
public:
    int minOperations(int n) {
        int target = n;
        int ans = 0;

        for (int i = 0; i < n; i++) {
            int value = 2 * i + 1;

            if (value < target) {
                ans += target - value;
            }
        }

        return ans;
    }
};
