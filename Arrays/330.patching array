class Solution {
public:
    int minPatches(vector<int>& nums, int n) {

        long long reach = 0;
        int patches = 0;
        int i = 0;

        // we need to cover [1 ... n]
        while (reach < n) {

            // use existing number
            if (i < nums.size() && nums[i] <= reach + 1) {

                reach += nums[i];
                i++;
            }
            else {

                // patch with reach + 1
                reach += (reach + 1);
                patches++;
            }
        }

        return patches;
    }
};
